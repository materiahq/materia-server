'use strict';

//var userEntityConfig = require('./entity/user')

class BasicAuth {
	constructor(app, config) {
		this.app = app

		/*if (config.entity && config.entity.user) {

		}
		let user = this.app.entities.get('user')
		if (user) {
		}
		else {
			this.app.entities.add('user')
		}*/
	}
}
