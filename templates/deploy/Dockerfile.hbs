FROM {{instance.image_base}}
MAINTAINER {{app.author}}

RUN mkdir -p /app

# invalidate cache
RUN uptime

COPY . /app

WORKDIR /app

ENV MATERIA_MODE production

EXPOSE {{instance.port}}
CMD ["yarn", "start"]